plugins {
    id 'com.android.application'
}

android {
    namespace 'com.mira.bridge'
    compileSdk 33

    defaultConfig {
        applicationId "com.mira.bridge"
        minSdk 24 // 👈 تم التعديل إلى 24 لحل تعارض mira-inventory
        targetSdk 33
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.9.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'

    // الموديولات الداخلية
    implementation project(':mira-core')
    implementation project(':mira-rfid')
    implementation project(':mira-ui')
    implementation project(':mira-inventory')
}
